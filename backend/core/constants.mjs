export const WidgetSizes = Object.freeze({
  XS: { width: 1, height: 1 },
  S: { width: 2, height: 1 },
  M: { width: 2, height: 2 },
  L: { width: 4, height: 2 },
});

export const DefaultGridConfig = Object.freeze({
  rows: 12,
  cols: 12,
});

export const DefaultThemeId = "cyber";

export const SupportedThemeIds = Object.freeze(["cyber", "fifa", "mission-control", "glass", "retro"]);
